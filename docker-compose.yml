# 业务说明：为本地联调提供 Redis 数据库，保持后台管理与前台展示使用同一份运行数据。
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data

volumes:
  redis-data:

